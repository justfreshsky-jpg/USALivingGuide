# Google Cloud configuration (set ONE of these — GOOGLE_CLOUD_PROJECT takes priority)
GOOGLE_CLOUD_PROJECT=your-gcp-project-id
# GCP_PROJECT=your-gcp-project-id  # fallback if GOOGLE_CLOUD_PROJECT is not set

VERTEX_LOCATION=us-central1
GEMINI_MODEL=gemini-1.5-flash

# Optional: provide a pre-obtained OAuth token instead of relying on the metadata server
# GOOGLE_OAUTH_ACCESS_TOKEN=

# Flask / Gunicorn
PORT=8080

# Optional: set to enable file logging (leave empty for stdout-only in Docker)
# LOG_DIR=logs
